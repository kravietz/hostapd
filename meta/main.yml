galaxy_info:
  author: Paweł Krawczyk
  description: Role to set up and configure `hostapd` using `systemd.service` that supports all configuration options.
  company: krvtz.net
  issue_tracker_url: https://github.com/kravietz/hostapd/issues
  license: GPL-3.0-only

  min_ansible_version: '2.1'

  platforms:
    - name: Ubuntu
      versions:
        - bionic
        - focal
    - name: Debian
      versions:
        - bullseye
        - buster
    - name: ArchLinux
      version:
        - any
    - name: EL
      version:
        - 7
    - name: Fedora
      version:
        - 25

  galaxy_tags:
    - hostapd
    - wifi
    - wpa_supplicant
    - linux

dependencies:
  - role: shibumi.systemd-conf
